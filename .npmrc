node-linker=hoisted
# Without this, `engines` is advisory: pnpm prints a warning and installs anyway.
# No other repo in this workspace sets it, which is why a Node-major drift
# between the host and the checks container went unnoticed.
engine-strict=true
