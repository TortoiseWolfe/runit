{
  "capturedFrom": "a LOCAL build of supabase/migrations/, verified group-for-group against the LIVE project qwusbxallkbzfladvgfx",
  "capturedAt": "2026-09-20",
  "verifiedAgainst": "Re-captured after #19 added `public.sole_host_events(uuid)` and `public.my_deletion_impact()` (functions 38->40). ONLY the functions group moved, which is the honest shape of this change: two read-only SQL functions, no column, no policy, no index, no trigger and no seed. PRODUCTION WAS RE-CHECKED RATHER THAN ASSUMED -- the two functions were applied to qwusbxallkbzfladvgfx first, then `schema-fingerprint.sql` was run against BOTH SIDES and all six groups compared: columns 130, functions 40, indexes 35, policies 37, tier_limits 4, triggers 19, every hash identical. A baseline refreshed without that comparison records a wish rather than a fact, which is why the tool says so on every failure. AND THE GRANTS WERE READ BACK, because the fingerprint cannot see one: `has_function_privilege` reports `my_deletion_impact` executable by authenticated and not by anon, and `sole_host_events` by neither -- it takes a uid as a parameter, so a client that could call it would hold an enumeration oracle. Lane E asserts that revoke; this records the deployed state matching it.",
  "howToRefresh": "pnpm schema:fingerprint --write, and only after re-checking production. See docs/lane-e.md and issue #49.",
  "schema": {
    "columns": {
      "n": 130,
      "fingerprint": "6c1e37fef9361b901be7bf7cfedf5e02"
    },
    "functions": {
      "n": 40,
      "fingerprint": "7b96abf23fc44d7c55efa767380df12e"
    },
    "indexes": {
      "n": 35,
      "fingerprint": "2f99bce6594a96ed068b86f337fc21a1"
    },
    "policies": {
      "n": 37,
      "fingerprint": "abc9802a01fe0e7fd9aa213438a663fe"
    },
    "tier_limits": {
      "n": 4,
      "fingerprint": "8bd890fb12fd2c60d91b6e19bd9bc517"
    },
    "triggers": {
      "n": 19,
      "fingerprint": "16e930773c652f6b6f9796a2ee58ed98"
    }
  }
}
